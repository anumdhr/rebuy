import 'package:flutter/material.dart';

class ProductNavBar extends StatelessWidget {
  const ProductNavBar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      bottomNavigationBar: BottomAppBar(



      ),
    );
  }
}
